# Glide — agent guide

**Read `CLAUDE.md` — it is the single source of truth for this project** (standing
context, architecture, session history, working agreements), and
`Glide-Session-Handoff-NEXT.md` for the current state + what to do next.

This file used to hold a synced copy of CLAUDE.md but drifted badly out of date
(frozen at Session 60 while the project moved on), which is worse than no copy —
so now it just points at the real thing.
